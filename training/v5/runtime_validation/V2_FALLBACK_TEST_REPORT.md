# FreshGuard Vision — Reversible V2 Fallback Verification

- **Default Setting**: `FRESHGUARD_VISION_MODEL=v2` (Resolves to 35-class production baseline).
- **Switching Mechanism**: Reversible runtime flag without server restarts or code mutations.
- **Fallback Verification**: **PASSED**
